# Build output
bin/
obj/
dist/
.publish-tmp/
.publish-test/
.publish-ci/
.neg-publish/
# CI UI-selftest 运行产物
ui-selftest-result.txt
out.txt
err.txt
# e2e 本地运行临时日志
e2e-*.log
# WiX v5 temp files generated next to the build source
.tmp-wix/
# WiX tool cache (defensive)
.wix/

# Test output
TestResults/

# Archived stale content (old releases, build artifacts, archived docs)
archive/

# IDE
.vs/
*.user
*.suo

# OS
Thumbs.db
.DS_Store
installer/*/out/

# pnpm artifacts (test-generated, must not be committed)
pnpm-lock.yaml
package.json
/node_modules/
pnpm-workspace.yaml

# 测试沙箱残留（SafeModeSandboxOutcomes / 真实 OS 测试在仓库内搭建的假 home，
# 含整棵 node_modules，绝不可入库——2026-08-22 git add -A 误扫事件后加入）
sandbox/
